<%= form_with(model: hold) do |form| %>
  <% if hold.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(hold.errors.count, "error") %> prohibited this hold from being saved:</h2>

      <ul>
        <% hold.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :type %>
    <%= form.text_area :type %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
